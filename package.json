{
  "name": "nodejs-app",
  "version": "1.0.0",
  "description": "Simple Node.js server-side application",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "lint": "echo \"No linting configured yet\"",
    "test": "echo \"No tests configured yet\" && exit 0",
    "build": "echo \"No build step needed\""
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}